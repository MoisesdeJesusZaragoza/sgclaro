<?php
      $host = "localhost";
      $user = "root";
      $pass = "";
      $db = "sgresident";
    
      $conexion = mysqli_connect($host, $user, $pass, $db);

      //$cerrar_cn = mysqli_close($conexion);
?>